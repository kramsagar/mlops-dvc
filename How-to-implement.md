1. dvc install
    pip install dvc
2. dvc init
    dvc init
3. dvc add <data-file-or-directory>
    dvc add data\wine_sample.csv
4. dvc remote add -d <remote-name> <remote-url> 
    dvc remote add -d myremotedvcmlops s3://
5. dvc-s3 install
    python -m pip install --upgrade pip
    python -m pip install dvc_s3

6. dvc push
    dvc push    

<video controls src="20251219-0846-49.9052651.mp4" title="Title"></video>